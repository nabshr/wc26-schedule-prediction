// ── Shared simulation configuration ────────────────────────────────────
// Change this one constant to update run count everywhere in the app.
export const DEFAULT_SIMULATION_RUNS = 50000;
export const SIMULATION_SEED = 2026;
export const MODEL_VERSION = 'Elo-Poisson-DC-v2.0';
